#!/usr/bin/bash

man which

which -a bash

mkdir --help

which -a python

python --version

which -a python3

python3 --version

which -a curl

curl --help

which -a nproc

nproc --help

nproc --all

which -a lscpu

lscpu --help
 
lscpu --version

lscpu --output-all
